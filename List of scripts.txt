Scripts for Marvik

PRIORITY SCRIPTS TO START WITH.
1. Validate ADT -> Check for problems that need to be corrected.
    - It checks that every single HTML element that encompasses text has a data-id. 
    - It also filters emojis that are outside of the translation system so the remain.

    TO BE ADDED:
    - This means if you have nested spans within a <p> it will be able to filter the paragraph out but only flag data-ids on the internal elements. 

2. Fix missing data-ids -> 
    - Adds data-ids to the internal object and the key to the JSON file.

3. Restructure text -> Puts all text strings in span elements. 
    - Wrapping text content in <span> tags with data-id attributes.
    - Moving data-id from <p> tags to <span> tags
    - Intelligently grouping related spans into paragraphs based on content analysis

NEXT TO WORK ON.
JSON regeneration - 
1. Regenerate image descriptions.
2. Regenerate eli5.
3. Regenerate easy-read.
2. Regenerate text translations.

TTS regeneration - 
1. Regenerate text tts. 


LATER 
4. Standardize headings. 
6. Standardize image text layouts.
5. Standardize html.

